export default class FastnElementMetadata {
    constructor({ elementName, description, properties }) {
        this.elementName = elementName;
        this.description = description;
        this.properties = properties;
    }
}
